type Mutation {
    createAccount(
        userName: String!
        password: String!
        email: String!
        firstName: String
        lastName: String
        bio: String
    ): Boolean!
}

type Query {
    something: String
}
